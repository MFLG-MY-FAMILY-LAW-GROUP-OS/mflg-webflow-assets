# mflg-webflow-assets
External Webflow CSS, JavaScript, and static front-end assets for MY FAMILY LAW GROUP PLLC.

## Production Intake

The active intake assets are:

- `js/mflg-intake.js`
- `css/mflg-intake.css`

Current production version:

- `3.5.0-consistency-guardrails`

Before release, run:

```bash
./scripts/check-intake-release.sh
```

Release and rollback instructions live in `RELEASE.md`.
